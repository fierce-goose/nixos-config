Usage:
```
git clone https://github.com/fierce-goose/nixos
cd nixos
cp config/* /etc/nixos/
cp home-manager/* ~/.config/home-manager/
cp wallpapers/* ~/wallpapers
sudo nixos-rebuild switch
home-manager switch
```
